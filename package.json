{
  "name": "ntut-csie-25",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "generate:cms": "node scripts/generate-cms-config.mjs",
    "dev": "npm run generate:cms && astro dev",
    "build": "npm run generate:cms && astro build",
    "preview": "npm run build && wrangler pages dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.1",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "wrangler": "^4.129.0"
  }
}
